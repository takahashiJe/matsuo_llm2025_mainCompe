vllm>=0.5.1
torch>=2.4.0
torchaudio>=2.4.0
torchvision>=0.19.0
transformers
peft>=0.12.0
accelerate
setuptools
# tritonはTORCH_SDPAを使う場合は不要
compressed-tensors
openai
xgrammar
bitsandbytes
fire
huggingface_hub

# uv pip install -r requirements.txt
# uv pip install flash-attn --no-build-isolation